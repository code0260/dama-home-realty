# 📚 الوثيقة الشاملة لمشروع Dama Home Realty

## 📋 جدول المحتويات

1. [نظرة عامة على المشروع](#نظرة-عامة-على-المشروع)
2. [البنية التقنية](#البنية-التقنية)
3. [المكونات الرئيسية](#المكونات-الرئيسية)
4. [قاعدة البيانات](#قاعدة-البيانات)
5. [واجهات برمجة التطبيقات (API)](#واجهات-برمجة-التطبيقات-api)
6. [واجهة المستخدم (Frontend)](#واجهة-المستخدم-frontend)
7. [لوحة الإدارة (Admin Panel)](#لوحة-الإدارة-admin-panel)
8. [الميزات والوظائف](#الميزات-والوظائف)
9. [الأمان](#الأمان)
10. [الأداء والتحسينات](#الأداء-والتحسينات)
11. [التنصيب والنشر](#التنصيب-والنشر)
12. [المشاكل والتحسينات المقترحة](#المشاكل-والتحسينات-المقترحة)

---

## نظرة عامة على المشروع

### الهدف

**Dama Home Realty** هو منصة شاملة لإدارة العقارات في دمشق، مصممة خصيصاً لربط المغتربين السوريين بالعقارات الموثوقة في وطنهم.

### التقنيات المستخدمة

#### Backend (الخادم)

-   **Laravel 11** - إطار عمل PHP
-   **Filament 3.2** - لوحة إدارة متقدمة
-   **Laravel Sanctum** - المصادقة للـ SPA
-   **Spatie Laravel Translatable** - دعم متعدد اللغات
-   **Spatie Laravel Permission** - نظام الصلاحيات
-   **Laravel Cashier (Stripe)** - معالجة المدفوعات
-   **MySQL 8.0+** - قاعدة البيانات

#### Frontend (واجهة المستخدم)

-   **Next.js 16** (App Router) - إطار عمل React
-   **TypeScript** - الأمان النوعي
-   **Tailwind CSS 4** - التصميم
-   **Shadcn UI** - مكونات واجهة المستخدم
-   **Lucide React** - الأيقونات
-   **Axios** - عميل HTTP
-   **Date-fns** - معالجة التواريخ

---

## البنية التقنية

### هيكل المشروع

```
dama-home-realty/
├── backend/                    # Laravel Backend
│   ├── app/
│   │   ├── Filament/          # موارد لوحة الإدارة
│   │   ├── Http/
│   │   │   ├── Controllers/Api/  # وحدات تحكم API
│   │   │   ├── Middleware/      # Middleware
│   │   │   ├── Requests/        # طلبات التحقق
│   │   │   └── Resources/       # موارد API
│   │   ├── Mail/               # قوالب البريد الإلكتروني
│   │   ├── Models/              # نماذج Eloquent
│   │   ├── Services/           # خدمات الأعمال
│   │   └── Notifications/       # الإشعارات
│   ├── database/
│   │   ├── migrations/          # هجرات قاعدة البيانات
│   │   ├── seeders/            # Seeders
│   │   └── factories/          # Factories
│   ├── routes/
│   │   ├── api.php             # مسارات API
│   │   └── web.php             # مسارات الويب
│   ├── config/                 # ملفات الإعدادات
│   └── public/                 # الملفات العامة
│
└── backend/frontend/           # Next.js Frontend
    ├── app/                    # صفحات App Router
    ├── components/             # مكونات React
    ├── lib/                    # أدوات و API Client
    ├── types/                  # أنواع TypeScript
    └── hooks/                  # React Hooks
```

### ملاحظة هامة

⚠️ **هناك مجلد `app/` في الجذر ومجلد `backend/app/` - يبدو أن هناك ازدواجية في البنية. المجلد الفعلي المستخدم هو `backend/app/`**

---

## المكونات الرئيسية

### 1. النماذج (Models)

#### Property (العقار)

-   **الحقول الرئيسية:**
    -   `uuid` - معرف فريد
    -   `slug` - رابط فريد
    -   `title` - العنوان (متعدد اللغات: en, ar)
    -   `description` - الوصف (متعدد اللغات)
    -   `price` - السعر
    -   `currency` - العملة (USD, SYP)
    -   `type` - النوع (rent, sale, hotel)
    -   `status` - الحالة (active, sold, rented, pending, draft)
    -   `neighborhood_id` - الحي
    -   `agent_id` - الوكيل
    -   `bedrooms`, `bathrooms`, `area_sqm` - المواصفات
    -   `is_verified`, `is_featured` - علامات
    -   `amenities` - المرافق (JSON)
    -   `images` - الصور (JSON)
    -   `wifi_password`, `door_code`, `house_rules`, `full_address` - معلومات المستأجر

#### Booking (الحجز)

-   **الحقول:**
    -   `property_id` - العقار
    -   `user_id` - المستخدم
    -   `check_in`, `check_out` - تواريخ الحجز
    -   `total_price` - السعر الإجمالي
    -   `amount_paid` - المبلغ المدفوع
    -   `payment_status` - حالة الدفع (pending, paid, partial, refunded)
    -   `booking_status` - حالة الحجز (pending, confirmed, cancelled, completed)
    -   `stripe_checkout_session_id` - معرف جلسة Stripe

#### User (المستخدم)

-   **الأدوار:**
    -   Super Admin
    -   Admin
    -   Staff
    -   Tenant
-   **الميزات:**
    -   Laravel Sanctum Authentication
    -   Spatie Permissions
    -   Laravel Cashier (Stripe)

#### Lead (العميل المحتمل)

-   **الحقول:**
    -   `name`, `email`, `phone`
    -   `type` - نوع الطلب (inquiry, tour_request, service_request)
    -   `property_id` - العقار المرتبط (اختياري)
    -   `message` - الرسالة

#### Agent (الوكيل)

-   معلومات الوكيل العقاري
-   ربط بالعقارات

#### Neighborhood (الحي)

-   معلومات الأحياء في دمشق
-   دعم متعدد اللغات

#### Service (الخدمة)

-   خدمات المنصة (إدارة العقارات، المساعدة القانونية، استقبال المطار)

#### Testimonial (الشهادات)

-   شهادات العملاء

#### Article (المقالات)

-   مقالات المدونة

---

## قاعدة البيانات

### الجداول الرئيسية

1. **users** - المستخدمون
2. **properties** - العقارات
3. **bookings** - الحجوزات
4. **leads** - العملاء المحتملون
5. **agents** - الوكلاء
6. **neighborhoods** - الأحياء
7. **services** - الخدمات
8. **testimonials** - الشهادات
9. **articles** - المقالات
10. **contracts** - العقود
11. **notifications** - الإشعارات
12. **activity_log** - سجل الأنشطة
13. **analytics_events** - أحداث التحليلات
14. **analytics_sessions** - جلسات التحليلات
15. **analytics_conversions** - التحويلات
16. **dashboard_layouts** - تخطيطات لوحة التحكم

### العلاقات (Relationships)

-   `Property` → `Neighborhood` (BelongsTo)
-   `Property` → `Agent` (BelongsTo)
-   `Property` → `Bookings` (HasMany)
-   `Property` → `Leads` (HasMany)
-   `Booking` → `Property` (BelongsTo)
-   `Booking` → `User` (BelongsTo)
-   `Booking` → `Contract` (HasOne)
-   `User` → `Bookings` (HasMany)

---

## واجهات برمجة التطبيقات (API)

### المصادقة (Authentication)

-   `POST /api/register` - التسجيل
-   `POST /api/login` - تسجيل الدخول
-   `POST /api/logout` - تسجيل الخروج
-   `GET /api/user` - معلومات المستخدم

### العقارات (Properties)

-   `GET /api/properties` - قائمة العقارات (مع فلترة)
-   `GET /api/properties/{identifier}` - تفاصيل عقار
-   `GET /api/properties/{identifier}/availability` - توفر العقار
-   `POST /api/properties` - إنشاء عقار (يتطلب مصادقة)
-   `PUT /api/properties/{id}` - تحديث عقار
-   `DELETE /api/properties/{id}` - حذف عقار

### الحجوزات (Bookings)

-   `GET /api/bookings` - قائمة الحجوزات (يتطلب مصادقة)
-   `POST /api/bookings` - إنشاء حجز
-   `GET /api/bookings/{id}` - تفاصيل حجز
-   `PUT /api/bookings/{id}` - تحديث حجز
-   `DELETE /api/bookings/{id}` - إلغاء حجز
-   `POST /api/bookings/{id}/checkout` - إنشاء جلسة دفع
-   `GET /api/bookings/{id}/payment/verify` - التحقق من الدفع

### المدفوعات (Payments)

-   **Stripe Integration** - معالجة المدفوعات
-   **Webhook** - `POST /api/webhooks/stripe` - استقبال أحداث Stripe

### لوحة الإدارة (Admin Dashboard)

-   `GET /api/admin/dashboard/stats` - إحصائيات
-   `GET /api/admin/dashboard/revenue` - الإيرادات
-   `GET /api/admin/dashboard/bookings` - الحجوزات
-   `GET /api/admin/dashboard/leads` - العملاء المحتملون
-   `GET /api/admin/dashboard/properties` - العقارات
-   `GET /api/admin/analytics/*` - التحليلات

### Rate Limiting (حدود الطلبات)

-   **Authentication**: 10 طلبات/دقيقة
-   **AI Routes**: 20 طلبات/دقيقة
-   **Contact/Lead**: 10 طلبات/دقيقة (منع السبام)
-   **Properties**: 60 طلبات/دقيقة
-   **Bookings**: 30 طلبات/دقيقة
-   **Payments**: 20 طلبات/دقيقة
-   **Webhooks**: 100 طلبات/دقيقة

---

## واجهة المستخدم (Frontend)

### الصفحات الرئيسية

1. **الصفحة الرئيسية** (`/`)

    - Hero Section
    - العقارات المميزة
    - الشهادات
    - آخر الأخبار
    - الإحصائيات

2. **قائمة العقارات** (`/properties`)

    - فلترة متقدمة
    - البحث
    - عرض الشبكة/القائمة

3. **تفاصيل العقار** (`/properties/[slug]`)

    - معرض الصور
    - معلومات مفصلة
    - نموذج الحجز
    - خريطة الموقع
    - معلومات الوكيل

4. **لوحة المستأجر** (`/portal`)

    - Dashboard Overview
    - الحجوزات النشطة
    - الخدمات المطلوبة
    - تاريخ الحجوزات
    - الإشعارات

5. **صفحة الحجز** (`/bookings/[id]/payment`)

    - ملخص الحجز
    - نموذج الدفع (Stripe)

6. **إضافة عقار** (`/list-property`)

    - نموذج متعدد الخطوات
    - حفظ كمسودة

7. **الخدمات** (`/services`)
8. **من نحن** (`/about`)
9. **اتصل بنا** (`/contact`)
10. **المقالات** (`/articles`)

### المكونات الرئيسية

-   **Navbar** - شريط التنقل
-   **Footer** - التذييل
-   **PropertyCard** - بطاقة عقار
-   **BookingForm** - نموذج الحجز
-   **PropertyFilters** - فلاتر البحث
-   **ImageGallery** - معرض الصور
-   **MapView** - عرض الخريطة

---

## لوحة الإدارة (Admin Panel)

### Filament Admin Panel

**الوصول:** `/admin`

### الموارد (Resources)

1. **Properties** - إدارة العقارات

    - CRUD كامل
    - دعم متعدد اللغات
    - رفع الصور
    - إدارة الحالة

2. **Bookings** - إدارة الحجوزات

    - عرض جميع الحجوزات
    - تحديث الحالة
    - عرض المدفوعات

3. **Leads** - إدارة العملاء المحتملين

    - استعلامات
    - طلبات الجولات
    - طلبات الخدمات

4. **Agents** - إدارة الوكلاء
5. **Neighborhoods** - إدارة الأحياء
6. **Services** - إدارة الخدمات
7. **Testimonials** - إدارة الشهادات
8. **Articles** - إدارة المقالات
9. **Activity Log** - سجل الأنشطة

### Widgets (الأدوات)

-   **BookingsChart** - مخطط الحجوزات
-   **RevenueChart** - مخطط الإيرادات
-   **LatestLeads** - آخر العملاء المحتملين
-   **PopularNeighborhoods** - الأحياء الشائعة

---

## الميزات والوظائف

### الميزات العامة (بدون تسجيل دخول)

✅ **عرض العقارات**

-   تصفح جميع العقارات
-   فلترة متقدمة (النوع، الحي، السعر، المرافق)
-   البحث
-   عرض تفاصيل كاملة

✅ **معلومات الوكيل**

-   بطاقات الوكلاء
-   التواصل عبر WhatsApp

✅ **جدولة الجولات الحية**

-   طلب جولة فيديو مباشرة

✅ **صفحة الخدمات**

-   إدارة العقارات
-   المساعدة القانونية
-   استقبال المطار

✅ **الشهادات**

-   شهادات العملاء

✅ **المقالات**

-   مدونة العقارات

### لوحة المستأجر (VIP Dashboard)

✅ **Dashboard نشط**

-   واجهة Concierge-style
-   شريط التقدم (Timeline)
-   معلومات الوصول الخاصة (WiFi، كود الباب، العنوان الكامل)
-   قواعد المنزل
-   تتبع الخدمات المطلوبة
-   إدارة الحجوزات

### ميزات الإدارة

✅ **إدارة كاملة للعقارات**
✅ **إدارة الوكلاء**
✅ **تتبع العملاء المحتملين**
✅ **تقويم الحجوزات**
✅ **معالجة المدفوعات (Stripe)**
✅ **إدارة الخدمات**
✅ **إدارة الشهادات**
✅ **التحليلات والإحصائيات**

### ميزات متقدمة

✅ **دعم متعدد اللغات** (الإنجليزية والعربية)
✅ **AI Search** - بحث ذكي بالذكاء الاصطناعي
✅ **AI Concierge** - مساعد ذكي
✅ **Real-time Notifications** - إشعارات فورية
✅ **Activity Logging** - تسجيل الأنشطة
✅ **Google Analytics Integration** - تكامل Google Analytics

---

## الأمان

### المصادقة والتفويض

-   **Laravel Sanctum** - مصادقة SPA
-   **Cookie-based Authentication** - مصادقة بالكوكيز
-   **CSRF Protection** - حماية CSRF
-   **Rate Limiting** - تحديد معدل الطلبات
-   **Spatie Permissions** - نظام الصلاحيات

### الأمان الإضافي

-   **Password Hashing** - تشفير كلمات المرور
-   **SQL Injection Protection** - حماية من SQL Injection (Eloquent ORM)
-   **XSS Protection** - حماية من XSS
-   **HTTPS** - تشفير الاتصال
-   **Security Headers** - رؤوس الأمان

### Stripe Security

-   **Webhook Signature Verification** - التحقق من توقيع Webhook
-   **Secure Payment Processing** - معالجة آمنة للمدفوعات

---

## الأداء والتحسينات

### قاعدة البيانات

-   **Indexes** - فهارس محسّنة
-   **Eager Loading** - تحميل علاقات محسّن (منع N+1)
-   **Query Optimization** - تحسين الاستعلامات

### Frontend

-   **Next.js 16** - Server-Side Rendering
-   **Image Optimization** - تحسين الصور
-   **Code Splitting** - تقسيم الكود
-   **Lazy Loading** - التحميل الكسول

### Caching

-   **Laravel Cache** - التخزين المؤقت
-   **Browser Caching** - تخزين المتصفح

---

## التنصيب والنشر

### المتطلبات

-   PHP 8.2+
-   Composer
-   Node.js 18+
-   MySQL 8.0+
-   Git

### متغيرات البيئة المهمة

```env
APP_NAME="Dama Home Realty"
APP_ENV=production
APP_DEBUG=false
APP_URL=https://damahomerealty.com

DB_CONNECTION=mysql
DB_HOST=127.0.0.1
DB_PORT=3306
DB_DATABASE=dama_home_realty
DB_USERNAME=root
DB_PASSWORD=

NEXT_PUBLIC_API_URL=https://damahomerealty.com/api

STRIPE_KEY=pk_live_...
STRIPE_SECRET=sk_live_...
STRIPE_WEBHOOK_SECRET=whsec_...

SANCTUM_STATEFUL_DOMAINS=damahomerealty.com,www.damahomerealty.com
```

### خطوات التنصيب

1. **Backend Setup**

```bash
cd backend
composer install
cp .env.example .env
php artisan key:generate
php artisan migrate
php artisan db:seed
```

2. **Frontend Setup**

```bash
cd backend/frontend
npm install
npm run build
```

3. **Run Servers**

```bash
# Backend
php artisan serve

# Frontend
npm run start
```

---

## المشاكل والتحسينات المقترحة

### ⚠️ المشاكل المكتشفة

#### 1. **ازدواجية في البنية**

-   يوجد مجلد `app/` في الجذر ومجلد `backend/app/`
-   **الحل:** حذف مجلد `app/` في الجذر إذا كان غير مستخدم

#### 2. **مجلد frontend فارغ**

-   يوجد مجلد `frontend/` فارغ في الجذر
-   **الحل:** حذفه أو توثيقه

#### 3. **منطق تداخل الحجوزات (Booking Overlap)**

-   في `Booking::hasOverlap()` استخدام `subDay()` و `addDay()` قد يسبب مشاكل في الحالات الحدية
-   **الحل المقترح:** مراجعة المنطق وتحسينه

#### 4. **TypeScript Build Errors**

-   `ignoreBuildErrors: true` في `next.config.js`
-   **الحل:** إصلاح أخطاء TypeScript بدلاً من تجاهلها

#### 5. **Migration Issue**

-   في migration `add_owner_fields_and_status_enum_to_properties_table.php` يتم تحديث جميع السجلات 'active' إلى 'pending'
-   **الحل:** مراجعة هذا المنطق - قد لا يكون مرغوباً

#### 6. **كثرة ملفات التوثيق**

-   أكثر من 150 ملف markdown
-   **الحل:** دمج الملفات المهمة وحذف المكررة

### 💡 تحسينات مقترحة

1. **إضافة Unit Tests** - اختبارات الوحدة
2. **تحسين Error Handling** - معالجة أفضل للأخطاء
3. **إضافة API Documentation** - توثيق API (Swagger/OpenAPI)
4. **تحسين Logging** - تحسين نظام السجلات
5. **إضافة Monitoring** - مراقبة الأداء (Sentry)
6. **تحسين SEO** - تحسين محركات البحث
7. **إضافة PWA** - Progressive Web App
8. **تحسين Accessibility** - تحسين إمكانية الوصول

---

## الخلاصة

**Dama Home Realty** هو مشروع شامل ومتقدم لإدارة العقارات مع:

-   ✅ بنية تقنية قوية
-   ✅ ميزات متقدمة
-   ✅ أمان عالي
-   ✅ واجهة مستخدم حديثة
-   ✅ لوحة إدارة متكاملة

**مع بعض التحسينات المطلوبة في:**

-   ⚠️ تنظيف البنية
-   ⚠️ إصلاح بعض المشاكل المنطقية
-   ⚠️ تحسين التوثيق

---

**تاريخ الإنشاء:** 2025-01-24  
**الإصدار:** 1.0  
**آخر تحديث:** 2025-01-24
