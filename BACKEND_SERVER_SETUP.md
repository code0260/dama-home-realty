# 🚀 إعداد وتشغيل الخادم الخلفي

# Backend Server Setup Guide

**التاريخ**: 23 نوفمبر 2025

---

## ⚠️ **المشكلة:**

الخادم الخلفي (Laravel) غير متاح على `http://localhost:8000`.

---

## ✅ **الحل:**

### **1. تشغيل الخادم الخلفي:**

افتح Terminal في مجلد `backend` وقم بتشغيل:

```bash
cd backend
php artisan serve
```

سيتم تشغيل الخادم على `http://localhost:8000`

---

### **2. التحقق من الخادم:**

افتح المتصفح وانتقل إلى:

-   `http://localhost:8000` - يجب أن ترى صفحة Laravel الافتراضية
-   `http://localhost:8000/api/properties` - يجب أن ترى JSON response

---

### **3. تشغيل Frontend و Backend معًا:**

**Terminal 1 (Backend):**

```bash
cd backend
php artisan serve
```

**Terminal 2 (Frontend):**

```bash
cd backend/frontend
npm run dev
```

---

## 🔧 **إعدادات Environment:**

### **Backend (.env):**

```env
APP_URL=http://localhost:8000
FRONTEND_URL=http://localhost:3000
```

### **Frontend (.env.local):**

```env
NEXT_PUBLIC_API_URL=http://localhost:8000/api
```

---

## 📝 **ملاحظات:**

1. ✅ تم تحسين معالجة الأخطاء - لن تظهر أخطاء كثيرة في Console
2. ✅ التطبيق سيعمل حتى لو كان الخادم غير متاح (لكن API calls ستفشل)
3. ✅ رسالة تحذير واحدة فقط ستظهر في Console عند عدم توفر الخادم

---

## 🎯 **الخلاصة:**

**لحل المشكلة:**

1. افتح Terminal في مجلد `backend`
2. شغّل: `php artisan serve`
3. تأكد أن الخادم يعمل على `http://localhost:8000`

**بعد ذلك، جميع الأخطاء ستختفي! ✅**
